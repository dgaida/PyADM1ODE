# PyADM1 Core Dependencies
matplotlib>=3.5.0
networkx>=2.6
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0
PyYAML>=6.0

# Differentiable ADM1 backend (pyadm1.core.adm1_torch).
# Required in CI: without it the torch tests are skipped via importorskip and
# adm1_torch.py is reported as 0 % covered, deflating the overall coverage.
torch>=2.0

# Optional: Development tools
# pytest>=7.0
# pytest-cov>=3.0
pytest-asyncio
# black>=23.0
# ruff>=0.5.0
# mypy>=1.0
